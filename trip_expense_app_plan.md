# 旅行収支管理アプリ（北欧ミニマル風モバイルWebアプリ）開発計画

ユーザー様のご要望に基づき、旅行の支出・予算をすっきりと管理できるモバイル向けのWebアプリケーションを開発します。

## 1. アプリケーション概要とコンセプト
* **コンセプト**: 「シンプルで美しい、旅の収支パートナー」
* **デザインテーマ**: 北欧風ミニマル（温かみのあるニュートラルカラー、セージグリーン、ソフトグレー、クリーンなフォント、ゆったりとした余白）
* **ターゲットデバイス**: スマートフォン優先（Mobile-First）の直感的で使いやすいUI

## 2. 主な機能一覧
1. **旅行（イベント）管理機能**
   - 複数の旅行を作成・管理（例：「京都旅行 2026」「北海道旅行」）
   - 各旅行ごとの「総予算」「期間」「メモ」の設定
2. **収支（支出）入力・管理機能**
   - 支出の追加・編集・削除
   - 金額、カテゴリ（食費、交通、宿泊、観光、お土産、その他）、日付、支払方法（現金、カード、電子マネー等）、メモの入力
3. **ダッシュボード & 統計分析**
   - 予算に対する使用金額の進捗ゲージ（「あと○円使えます」）
   - カテゴリ別の支出割合（円グラフや美しいスタックバーチャート）
   - 日別の支出推移
4. **PDFエクスポート機能**
   - 旅行のサマリー、カテゴリ別統計、支出明細テーブルを、印刷・PDF保存に最適化された美しいレイアウトで出力（ブラウザの印刷機能を利用し、A4用紙やPDFに美しく収まるようにCSS設計）
5. **データ永続化**
   - `localStorage` を利用したローカル保存（インターネット不要、データはブラウザ内に安全に保管）
   - データのバックアップ（JSON形式でのエクスポート/インポート）

## 3. 技術スタック
* **フロントエンド**: React (TypeScript) + Vite
* **スタイリング**: Vanilla CSS (CSS Modules またはグローバルCSS＋CSS変数で、軽量かつ柔軟な北欧風デザインシステムを構築)
* **アイコン**: Lucide React (シンプルで美しいオープンソース of ラインアイコン)
* **グラフ表示**: カスタムSVG/CSS（軽量で外部ライブラリ不要な円グラフ・ゲージ）
* **PDFエクスポート**: `@media print` CSSルールを用いた、ブラウザ印刷機能とのシームレスな統合

## 4. 画面構成（SPA）
モバイル画面に最適化したビュー切り替え方式:
1. **旅行一覧ビュー (Trip List)**:
   - 過去および現在の旅行カード一覧。新規旅行の追加。
2. **旅行詳細ダッシュボード (Trip Dashboard)**:
   - 予算進捗バー、総支出、カテゴリ別グラフ。
   - 支出のクイック追加ボタン。
3. **履歴・明細ビュー (Expense History & List)**:
   - 登録されたすべての支出を日付順にリスト表示。
   - カテゴリや支払方法でのフィルタリング。
4. **設定・データ管理ビュー (Settings)**:
   - 全データのバックアップ/復元、旅行の編集。

---

## 5. 開発ロードマップと実装ステップ

### ステップ1: プロジェクトのセットアップ
1. Vite + React + TypeScript のプロジェクト初期化
2. フォルダ構造の整備と必要なパッケージ（`lucide-react` 等）のインストール
3. 北欧風デザインのカラーパレット、CSS変数の定義 (`index.css`)

### ステップ2: データモデルとLocalStorageヘルパーの実装
- `types.ts` での旅行 (`Trip`) および支出 (`Expense`) の型定義
- LocalStorageへのロード/セーブを行うHooksまたはユーティリティの実装

### ステップ3: UIコンポーネント & デザインシステムの実装
- モバイル用のナビゲーション、ボタン、入力フォーム、モーダル等の基本コンポーネント作成
- SVGを用いたドーナツチャート（カテゴリ比率）と予算進捗ゲージの実装

### ステップ4: 主要画面の実装
1. 旅行一覧・新規作成画面
2. 旅行詳細（ダッシュボード）画面
3. 支出一覧（履歴・追加・編集）画面

### ステップ5: PDFエクスポート（印刷用CSS）の実装
- 印刷時に不要なナビゲーションを隠し、綺麗にA4に収まるテーブルレイアウトとサマリーの印刷用表示をCSSで実装。
- 「PDFとして保存/印刷」ボタンをクリックしたときに `window.print()` を走らせる仕組みの構築。

### ステップ6: 動作確認とポーリッシュ
- iOS/Androidライクなレスポンシブ動作の確認。
- データのバックアップ/復元機能の動作検証。
- 細かなアニメーションとUIの微調整。
